<h1 class="page-heading">All Listings</h1>
<div class="row">
	<div class="col-md-12">
		<%= link_to 'New', new_listing_path, {class: 'btn btn-primary btn-md'} %>
	</div>
</div>

<div class="row">
	<div class="col-md-12">
		<% if @listings.length == 0 %>
			<p class="lead">No listings found </p>
		<% else %>
			<table class="table table-bordered table-striped table-responsive">
				<thead>
					<tr>
						<td>Title</td>
						<td>Description</td>
						<td>Price</td>
						<td></td>
						<td></td>
						<td></td>
						<td></td>
					</tr>
				</thead>
				<tbody>
				<% @listings.each do |listing| %>
					<tr>
						<td><%= listing.title %></td>
						<td><%= listing.description %></td>
						<td><%= listing.price %></td>
						<td><%= link_to 'Features', listing_features_path(listing) %></td>
						<td><%= link_to 'Pictures', listing_pictures_path(listing) %></td>
						<td><%= link_to 'Edit', edit_listing_path(listing)  %></td>
						<td><%= link_to 'Delete', listing, method: :delete, data:{confirm: 'Are you sure?'} %></td>
					</tr>
				<% end %>
				</tbody>
			</table>
		<% end %>
	</div>
</div>